.env
.env.*
!.env.example
node_modules/
.DS_Store

# Private Recherche/Notizen - nie committen
_local/

# Vendored AI-skill files (lokal installieren: npx impeccable install)
.cursor/skills/
.cursor/hooks.json
.github/skills/
.github/hooks/

# Next.js / Build-Artefakte
/.next/
/out/
*.tsbuildinfo

# Run-Artefakte und JSON-Store (SPEC §0) — Fixtures sind versioniert
data/*
!data/fixtures/

# Brand-Daten bleiben lokal und privat (Repo ist öffentlich) — nur die
# fiktive Beispiel-Brand ist versioniert
brands/*
!brands/creators-demo/

# Worktrees (AGENTS.md Workflow)
.worktrees/
