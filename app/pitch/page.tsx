import { notFound } from "next/navigation";

// Deck offline while the final version is being designed. Restore from git
// history once approved: git log --oneline -- app/pitch/page.tsx
export default function PitchPage() {
  notFound();
}
